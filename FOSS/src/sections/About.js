import styled from "styled-components";

const About = () => {
  return (
    <StyledAbout>
      <p>About</p>
    </StyledAbout>
  );
};

const StyledAbout = styled("section")``;

export default About;
